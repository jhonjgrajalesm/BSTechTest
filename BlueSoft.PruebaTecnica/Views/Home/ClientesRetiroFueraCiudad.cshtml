@using BlueSoft.PruebaTecnica.Entities
<h1>Listado de clientes por transaccion mayor a 1 millon y con ubicacion diferente a la cuenta origen</h1>
<table class="table table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>Nombre</th>
            <th>Cuenta</th>
            <th>Fecha</th>
            <th>Valor</th>
            <th>Tipo</th>
            <th>Ubicacion Transaccion</th>
            <th>Ubicacion Cuenta</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in (ViewBag.ClientesRetiroFueraCiudad as List<TransaccionCliente>))
        {
            <tr>
                <td>@item.NombreCliente</td>
                <td>@item.NroCuenta</td>
                <td>@item.FechaTransaccion.ToString("dd/MM/yyyy HH:mm:ss")</td>
                <td>@item.Valor.ToString("C")</td>
                <td>@(item.Consignacion ? "Consignacion" : "Retiro")</td>
                <td>@item.Ubicacion</td>
                <td>@item.UbicacionOrigenCuenta</td>
            </tr>
        }
    </tbody>
</table>